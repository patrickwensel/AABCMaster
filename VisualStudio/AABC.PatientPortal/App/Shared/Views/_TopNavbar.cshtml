<div class="row border-bottom">
    <nav class="navbar navbar-static-top" role="navigation" style="margin-bottom: 0">
        <div class="navbar-header">
            <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#"><i class="fa fa-bars"></i> </a>
            @*<form role="search" class="navbar-form-custom" method="post" action="">
                <div class="form-group">
                    <input type="text" placeholder="Search for something..." class="form-control" name="top-search" id="top-search">
                </div>
            </form>*@
        </div>

        <ul class="nav navbar-top-links navbar-right">
            <li>
                <span class="m-r-sm text-muted welcome-message" data-i18n="topnav.welcometext">Welcome to the Applied Behavioral Parent Portal</span>
            </li>

            @*<li class="dropdown">
                <a class="dropdown-toggle count-info" data-toggle="dropdown" href="#">
                    <i class="fa fa-bell"></i> 
                    @if (Html.App().Alerts.Count > 0) { <span class="label label-primary">@Html.App().Alerts.Count</span> }
                </a>
                <ul class="dropdown-menu dropdown-alerts">

                    @if (Html.App().Alerts.Count == 0) {
                        <li>
                            <a>
                                <div>
                                    <i class="fa fa-check fa-fw"></i> You're Good!  Nothing for now...
                                    <span class="pull-right text-muted small"> </span>
                                </div>
                            </a>
                        </li>
                        <li class="divider"></li>
                    }

                    @foreach (var item in Html.App().Alerts.AlertDropdownListItems) {

                        <li>
                            <a href="@item.Link">
                                <div>
                                    <i class="fa @item.FontAwesomeClass fa-fw"></i> @item.Message
                                    <span class="pull-right text-muted small">@item.Timestamp</span>
                                </div>
                            </a>
                        </li>
                        <li class="divider"></li>
                    }
                    
                    <li>
                        @if (Html.App().Alerts.Count == 0) {
                            <div class="text-center link-block text-disabled">
                                
                                    <strong>See All Alerts</strong>
                                    <i class="fa fa-angle-right"></i>
                                
                            </div>
                        } else {
                            <div class="text-center link-block">
                                <a href="/Alerts">
                                    <strong>See All Alerts</strong>
                                    <i class="fa fa-angle-right"></i>
                                </a>
                            </div>
                        }
                        
                    </li>
                </ul>
            </li>*@
            <li>
                <a href="@Url.Action("Logout", "Account")">
                    <i class="fa fa-sign-out"></i> Log out
                </a>
            </li>
        </ul>

    </nav>
</div>